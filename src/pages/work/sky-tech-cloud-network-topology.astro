---
const diagrams = [
  {
    title: 'Guangzhou Office Network Topology',
    description:
      'Original Guangzhou office topology from the Sky-Tech Cloud period, showing carrier uplinks, firewall pairs, distribution switches, access switches, wireless access points, DMZ, and office network zones.',
    image: '/images/work/sky-tech-topology/guangzhou-office-topology.png',
  },
  {
    title: 'Cloud Operations Platform Logical Network',
    description:
      'Original logical architecture for the cloud service operations management platform, covering public access, security controls, load balancing, service VLANs, database zones, and storage fabric connectivity.',
    image: '/images/work/sky-tech-topology/cloud-operations-platform.png',
  },
  {
    title: 'APAC BSS Network Topology',
    description:
      'Original APAC BSS topology showing firewalls, WAF, SLB, core switching, office connectivity, Beijing connectivity, and service VLAN segmentation.',
    image: '/images/work/sky-tech-topology/apac-bss-topology.png',
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="Original Sky-Tech Cloud topology diagrams from Dongxu Allen Hu's 2014-2015 network engineering experience."
    />
    <meta name="generator" content={Astro.generator} />
    <title>2014-2015 Sky-Tech Topology | Dongxu Allen Hu</title>
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href="/">Dongxu (Allen) Hu</a>
      <nav aria-label="Main navigation">
        <a href="/#experience">Experience</a>
        <a href="/#projects">Case Studies</a>
        <a href="/photography/">Photography</a>
      </nav>
    </header>

    <main>
      <section class="hero">
        <p class="eyebrow">Topology Archive</p>
        <h1>2014-2015 Sky-Tech Cloud Network Topology</h1>
        <p>
          Original network diagrams from the Sky-Tech Cloud Cisco-TCL joint venture period. These diagrams document the
          Guangzhou office network, APAC BSS environment, and cloud operations platform logical network behind the
          infrastructure delivery work.
        </p>
      </section>

      <section class="gallery" aria-label="Original Sky-Tech topology diagrams">
        {diagrams.map((diagram, index) => (
          <article class={`diagram-card ${index === 0 ? 'featured' : ''}`}>
            <div class="diagram-copy">
              <p class="eyebrow">Original Diagram</p>
              <h2>{diagram.title}</h2>
              <p>{diagram.description}</p>
            </div>
            <a class="diagram-image" href={diagram.image} target="_blank" rel="noreferrer">
              <img src={diagram.image} alt={diagram.title} loading={index === 0 ? 'eager' : 'lazy'} />
            </a>
          </article>
        ))}
      </section>
    </main>
  </body>
</html>

<style>
  :root {
    color-scheme: light;
    --bg: #f7f8f3;
    --ink: #18211f;
    --muted: #64716c;
    --line: #d9ded7;
    --panel: #ffffff;
    --accent: #0f766e;
    --accent-dark: #115e59;
    font-family:
      Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  * {
    box-sizing: border-box;
  }

  body {
    background: var(--bg);
    color: var(--ink);
    margin: 0;
  }

  a {
    color: inherit;
    text-decoration: none;
  }

  .site-header {
    align-items: center;
    backdrop-filter: blur(18px);
    background: color-mix(in srgb, var(--bg) 84%, transparent);
    border-bottom: 1px solid var(--line);
    display: flex;
    justify-content: space-between;
    padding: 18px clamp(20px, 5vw, 72px);
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .brand {
    font-weight: 800;
  }

  nav {
    color: var(--muted);
    display: flex;
    font-size: 0.94rem;
    gap: 22px;
  }

  main {
    margin: 0 auto;
    max-width: 1900px;
    padding: 56px clamp(20px, 4vw, 72px) 80px;
  }

  .hero {
    border-bottom: 1px solid var(--line);
    padding: 44px 0 38px;
  }

  .eyebrow {
    color: var(--accent-dark);
    font-size: 0.78rem;
    font-weight: 900;
    letter-spacing: 0;
    margin: 0 0 12px;
    text-transform: uppercase;
  }

  h1 {
    font-size: clamp(3rem, 7vw, 6.2rem);
    line-height: 0.95;
    margin: 0 0 22px;
    max-width: 1280px;
  }

  h2 {
    font-size: clamp(1.6rem, 2vw, 2.4rem);
    line-height: 1.15;
    margin: 0 0 12px;
  }

  p {
    color: var(--muted);
    font-size: 1.12rem;
    line-height: 1.72;
    margin: 0;
  }

  .hero p {
    max-width: 1080px;
  }

  .gallery {
    display: grid;
    gap: 26px;
    padding-top: 34px;
  }

  .diagram-card {
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: 8px;
    box-shadow: 0 18px 40px rgba(24, 33, 31, 0.06);
    display: grid;
    gap: 22px;
    grid-template-columns: minmax(280px, 0.34fr) minmax(0, 1fr);
    padding: 24px;
  }

  .diagram-card.featured {
    grid-template-columns: 1fr;
  }

  .diagram-copy {
    align-content: start;
    display: grid;
    gap: 10px;
  }

  .diagram-image {
    background: #ffffff;
    border: 1px solid var(--line);
    border-radius: 6px;
    display: block;
    min-width: 0;
    overflow: auto;
  }

  .diagram-image img {
    display: block;
    height: auto;
    min-width: 900px;
    width: 100%;
  }

  @media (max-width: 900px) {
    .site-header {
      align-items: flex-start;
      flex-direction: column;
      gap: 14px;
    }

    nav {
      flex-wrap: wrap;
    }

    main {
      padding-top: 28px;
    }

    .diagram-card {
      grid-template-columns: 1fr;
      padding: 18px;
    }

    .diagram-image img {
      min-width: 760px;
    }
  }
</style>
